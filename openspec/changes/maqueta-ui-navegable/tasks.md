## 1. Modo de datos, sesión y store de la demo

- [x] 1.1 Crear `lib/data/mode.ts` (`NEXT_PUBLIC_API_MODE`, por defecto `mock`) y documentar `NEXT_PUBLIC_API_MODE`/`NEXT_PUBLIC_API_URL` en `.env.example`, `apps/web/Dockerfile` (build args) y `docker-compose.yml` (Req: Prototipo navegable en modo simulado; RNF-002, RNF-008)
- [x] 1.2 Crear `lib/auth/session.tsx` (`SessionProvider`/`useSession`, `useSyncExternalStore` sobre `localStorage`, `hasPermission`) con la misma matriz de roles/permisos que `apps/api/app/seed/reference.py`; verificar con `lib/data/masking.test.ts` (usa `hasPermission`) y navegación manual (Req: Prototipo navegable…; RF-039)
- [x] 1.3 Crear `lib/data/mock-store.tsx` (`MockStoreProvider`/`useMockStore`, `useReducer`): aprobar/rechazar sugerencia de IA, fusionar/marcar distinto duplicado, decidir fila y aplicar lote de importación, registrar movimiento/verificación, corregir código I, editar pieza — cada acción agrega una entrada a `auditLog` sin modificar el historial existente; verificar recorriendo el flujo en el navegador (aprobar IA, fusionar duplicado, decidir fila) y comprobando que la Auditoría de la ficha refleja la acción (Req: Prototipo navegable…; RN-005, RN-009)
- [x] 1.4 Crear `lib/data/masking.ts` (`maskPieceForRole`, restringe ubicación exacta/comodante/convenio de comodato según permiso); verificar con `lib/data/masking.test.ts` (4 casos) (RF-041)
- [x] 1.5 Componentes compartidos: `components/ConfirmButton.tsx` (confirmación + motivo obligatorio para acciones irreversibles), `components/Badges.tsx` (régimen de tenencia, alertas, candado, estado), `components/AppShell.tsx` (nav filtrada por permiso, badge de modo demostración), `components/RequireSession.tsx` (redirección a `/` sin sesión, con lectura directa de `localStorage` para evitar la carrera de hidratación) (RNF-010, RF-039)
- [x] 1.6 Crear `lib/data/pieces.ts` (`fetchPieces`, `fetchPieceDetail`, modo mock/live) tipado contra el contrato real de `contratos-api-borrador` para las únicas lecturas de piezas ya implementadas (listado con `q`, ficha por id); verificar con `tsc --noEmit` (rama `live` compila contra `schema.d.ts`) y `lib/data/pieces.test.ts` (rama mock, 3 casos). Ninguna pantalla lo consume todavía — pendiente explícito, ver `design.md` D4 (RNF-009)

## 2. Datos sintéticos (`lib/fixtures/`)

- [x] 2.1 `reference-data.ts`, `locations.ts`, `collections.ts`: roles/permisos/usuarios, vocabularios/términos, tipos de identificador (copiados de `apps/api/app/seed/reference.py`), ubicaciones jerárquicas y colecciones (mismas siglas que `apps/api/app/seed/synthetic.py`: MMZ, RA, RAB, MBB, AJB en comodato, LRM) (RN-010, RF-010, RF-016)
- [x] 2.2 `pieces.ts`: 14 piezas tipadas contra `ApiSchemas["PieceDetail"]`, reproduciendo el "caos de codificación" (códigos concatenados en una celda, mayúsculas/minúsculas y puntos inconsistentes, ~sin código I, comodato con I asignado por error y corregido, préstamo temporal, conjunto/componente, código ilegible), con `alertsForPiece` (RF-019/RF-030 en `calidad-datos`), fotos placeholder generadas sin red (`placeholder-image.ts`), movimientos, datos de origen; verificar con `lib/fixtures/pieces.test.ts` (7 casos: RN-002, RN-003, RN-004, alertas, búsqueda) (RF-004, RF-005, RF-007, RF-008, RF-009, RF-013, RF-017, RF-023, RN-002..004)
- [x] 2.3 `audit.ts`, `ai-suggestions.ts`, `imports.ts`, `duplicates.ts`, `kpis-reports.ts`: bitácora inicial, sugerencias de IA (pendiente/aprobada/rechazada, RIA-01/03/04), lote y filas de importación con clasificación y diff, candidatos a duplicado (pieza-pieza y pieza-fila), KPIs y 4 reportes calculados a partir del catálogo; verificar manualmente que los KPI del tablero coinciden con los conteos de `computeIncompletePieces` (RF-021..030, RF-033..035, RF-040, RIA-01..04)
- [x] 2.4 `index.ts`: barrel + `listPieces` (filtros AND + `q`, RF-031/032), `searchPieces`, `toSummary`, accesores por pieza (media/movimientos/origen/IA); verificar con `lib/fixtures/pieces.test.ts` (filtros combinados y búsqueda por código sucio vs. normalizado)

## 3. Las 11 pantallas

- [x] 3.1 Pantalla 1 — Login (`app/page.tsx`): selector de persona/rol sintético, rol "Consulta externa/investigador" visible pero deshabilitado (RF-042); redirección a `/inicio` si ya hay sesión (RF-039)
- [x] 3.2 Pantalla 2 — Inicio/tablero (`app/inicio/page.tsx`): KPIs de completitud, últimas cargas, pendientes de duplicados/IA, tabla de incompletas con enlaces (RF-035, RF-030, RIA-01..05)
- [x] 3.3 **Pantalla 3 (prioridad)** — Búsqueda (`app/busqueda/page.tsx`): barra única + filtros laterales AND (colección/categoría/material/estado/tenencia/incompletas), tarjetas de resultado, exportar a Excel (simulado); `useSearchParams` + `Suspense` para `?incompleteOnly=1` (RF-031, RF-032, RF-036)
- [x] 3.4 **Pantalla 4 (prioridad)** — Ficha de pieza (`app/piezas/[id]/page.tsx`): 7 pestañas (Datos generales, Identificadores con candado y corrección auditada, Fotografías con restricciones de uso, Ubicación e historial con registro de movimiento/verificación, Datos de origen, Auditoría, Sugerencias IA), badges de completitud/régimen, enmascarado por rol (RF-006, RF-013, RF-016, RF-017, RF-023, RF-040, RF-041, RN-002)
- [x] 3.5 Pantalla 5 — Editor de pieza (`app/piezas/[id]/editar/page.tsx`): validación en tiempo real (denominación obligatoria ≥3 caracteres, descripción ≤600), sin tocar el código I (RF-043, RN-002)
- [x] 3.6 **Pantalla 6 (prioridad)** — Asistente de importación (`app/importacion/page.tsx`): 6 pasos (subir → mapeo → normalización/validación → previsualización con diff y clasificación → aprobación → bitácora con descarga simulada de rechazos), decisiones por fila (aceptar/excluir/rechazar con motivo) (RF-021..029)
- [x] 3.7 **Pantalla 7 (prioridad)** — Cola de duplicados (`app/duplicados/page.tsx`): comparación lado a lado (incluye un caso pieza-vs-fila de importación), fusionar/marcar distinto con motivo obligatorio, nunca elimina ninguna pieza (RF-030, RN-005)
- [x] 3.8 **Pantalla 8 (prioridad)** — Revisión de sugerencias de IA (`app/ia/sugerencias/page.tsx`): texto/metadatos de entrada vs. propuesta, edición del JSON antes de aprobar, aprobar/rechazar con motivo, ninguna se aplica sin esta revisión (RIA-01..05, RN-009)
- [x] 3.9 Pantalla 9 — Reportes (`app/reportes/page.tsx`): inventario general, por colección, por ubicación, incompletas, exportación completa (simulada) (RF-033..036, RF-044)
- [x] 3.10 Pantalla 10 — Administración (`app/administracion/page.tsx`): usuarios/roles, vocabularios, tipos de identificador, ubicaciones, cada pestaña gateada por su permiso (RF-039, RN-010, RF-016)
- [x] 3.11 Pantalla 11 — Vista móvil de depósito (`app/deposito/page.tsx`): una columna, botones grandes, buscar por código, ver foto/ubicación, registrar movimiento o verificación física; verificada a 375 px de ancho (RNF-001)

## 4. Documentación y verificación

- [x] 4.1 `docs/maqueta/recorrido-demo.md`: guion de ~10 minutos con las preguntas a validar por pantalla, referenciando `docs/preguntas-contraparte.md` sin duplicarlas
- [x] 4.2 Pruebas: `lib/fixtures/pieces.test.ts` (7), `lib/data/masking.test.ts` (4), `lib/data/pieces.test.ts` (3) — total `npm run test:web` 23/23 en verde; sin pruebas de componentes React (el proyecto no tiene Testing Library configurado) — verificación de las 11 pantallas hecha manualmente en el navegador (login, KPIs, búsqueda con filtros, ficha con sus 7 pestañas y corrección auditada de I, aprobar sugerencia de IA, previsualización de importación con diff/validación, reportes, administración, depósito a 375 px)
- [x] 4.3 `npm run lint` (api + ai + web), `npm run test` (237+20+23) y `npm run build:web` en verde; `openspec validate --all --strict` en verde
- [x] 4.4 Smoke test sin contenedores: `next build` + `next start` local, `curl` 200 en las 11 rutas + `/estado`, recorrido manual con el navegador de Claude Code (login → inicio → búsqueda → ficha → importación → duplicados → sugerencias IA → reportes → administración → depósito móvil); servidor detenido al terminar
- [x] 4.5 Registrar supuestos nuevos en `docs/preguntas-contraparte.md` y actualizar `docs/estado-arranque.md`
